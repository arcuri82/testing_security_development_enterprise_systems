
spring:
  datasource:
    url: jdbc:h2:mem:db;DB_CLOSE_DELAY=-1
    driver-class-name: org.h2.Driver
  jpa:
    database: h2
    show-sql: true
  cloud:
    consul:
      enabled: false
  rabbitmq:
    auto
  main:
    allow-bean-definition-overriding: true
  session:
    store-type: none


cardServiceAddress: cards-service
