jsf:
  projectStage: Production


spring:
  session:
    store-type: hash_map
  datasource:
    driver-class-name: "org.postgresql.Driver"
  jpa:
    show-sql: false
    hibernate:
      ddl-auto: "update"


security:
  sessions: ALWAYS

